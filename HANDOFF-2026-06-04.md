# BetaDeps session handoff — 2026-06-04

Comprehensive context for resuming work on BetaDeps from where this session left off. Read top to bottom; the "Resume from here" section at the end has the exact next step.

---

## 1. Current state of the world

- **v0.8.1 is shipped** to GitHub (tag + release) and Nexus (zip + sticky post + description + changelog + file description). The session retagged v0.8.1 to fold in this session's additions on top of the original 5-folder-layout hotfix.
- **Local working tree** has uncommitted in-progress code for the "every mod on one page" rewrite (slices 4a + 4b — see section 4 below). Build is clean. Slice 4c hit a documented architectural blocker.
- **Bug inbox** has been triaged through 04 Jun; major patterns identified, several still need action (section 5).

---

## 2. What v0.8.1 ships (already live)

### Code changes vs the v0.8.0 final commit (`bbba535`)

| Area | File | What changed |
|---|---|---|
| Zip packaging | `scripts/Build-Phase1.ps1` | 5-folder layout (BetaDeps + 4 BUTR deps as siblings, not nested). Linux-compatible zip entries (forward-slash names via `ZipArchive.CreateEntry`, replacing `Compress-Archive` which wrote backslashes). |
| BLSE filter | `src/BetaDeps.Harmony/BetaDepsHarmonySubModule.cs` | `OnSubModuleLoad` unconditionally writes `CREST_SHOW_STUBS=1` to User-scope env var. Idempotent — skips registry write when already set. BLSE picks it up on next launcher launch. |
| MO2 detect | `src/BetaDeps.Foundation/MO2Bootstrap.cs` (new) | USVFS detection via 3 signals (`usvfs_x64.dll` in process modules, parent `ModOrganizer.exe`, env vars `MOD_ORGANIZER_INST_PATH` / `USVFS_PROCESS_ID`). Resolves real on-disk game `Modules\` via `Process.MainModule.FileName` which the kernel sets before USVFS hooks install. `BootstrapAliasFolders` redirects materialisation target when MO2 detected. |
| Presets | `src/BetaDeps.MCM/Settings/SettingsStorage.cs` | New file-system layer: `ResolvePresetsDirectory`, `ResolvePresetPath`, `EnumeratePresets`, `SavePreset`, `LoadPresetIntoLiveFile`, `DeletePreset`, `SanitisePresetName`. Snapshots live at `Documents\Mount and Blade II Bannerlord\Configs\ModSettings\<SettingsId>\Presets\<name>.json`. Same JSON format as the live `Global\<id>.json` so users can hand-author / share / version-control. |
| Presets UI | `src/BetaDeps.MCM/UI/PrefabExtensions/OptionsVMMixin.cs` | Added `PresetCycle{Text,Visible}` data-source properties, `ExecutePresetCycle{Next,Prev,Apply}` methods, `RebuildPresetCycle(settingsId)` helper. Cycle entries: `(Current settings)` + saved presets + `(Save current as new...)`. Apply on save sentinel opens text input; apply on a preset name copies file → live, reloads in-memory singleton, calls `SelectMod` to re-bind UI. `PromptSavePresetName` flushes in-memory singleton to disk first via `SettingsStorage.Save(entry.Instance, settingsId)` so the snapshot captures what the user sees, not stale disk state. |
| Presets prefab | `src/BetaDeps.MCM/UI/PrefabExtensions/OptionsPrefabExtensions.cs` | `Preset: [<] [name] [>] [Apply]` row inserted at the top of each mod's settings panel, between the mod-name header and the property list. Gated on `@PresetCycleVisible`. |
| MCM toggle removal | `src/BetaDeps.MCM/Settings/BetaDepsSettings.cs` (deleted) | Removed in favor of unconditional CREST_SHOW_STUBS write. |

### Nexus / Patreon artifacts (this session, in `C:\dev\bannerlord\`)

| File | Status | Where it goes |
|---|---|---|
| `nexus-description.md` | Updated with "Three ways to install" section (Full BetaDeps / raw BUTR-equivalent stack / just Harmony) + BLSE auto-unhide note + Vortex + MO2 mentions | Description tab on Nexus |
| `nexus-changelog-v0.8.1.md` | Updated to include all 4 hotfix items (5-folder layout, BLSE auto-unhide, MO2 detect, Linux zip, presets, raw-stack mode) | Changelog box when uploading the v0.8.1 zip |
| `nexus-file-description-v0.8.1.md` | 138-char Files-tab blurb (under 150 char limit) | File description on the v0.8.1 zip upload |
| `nexus-sticky-post-v0.8.1.md` (new) | Sticky post replacing v0.7.5 with v0.8.1 headlines + per-user shoutouts | Edit current sticky on Posts tab |
| `nexus-short-description-v0.8.1.md` (new) | Search-snippet one-liner | Short description box |
| `patreon-v0.8.1-post.md` (new) | Full Patreon community post | New public post on patreon.com/trashpanda62 |

---

## 3. Inbox triage — what's still open

GitHub: 0 open issues (3 closed: #1, #5, #6, no new comments since 26 May).

Nexus Posts/Bugs tabs, last 7 days (compiled this session):

**P0 (confirmed by multiple reporters):**

1. **MCM dropdown impossible to change** — bakunine (30 May), SaxonSloth (31 May), Martin530 (02 Jun), 501stHANGY (02 Jun). Specific symptoms: AI-Influence API key / OpenRouter setup, Titles, Lowborn config options, any MCM dropdown control. Selftest shows `Cancel: 0/8 mods passed Cancel semantics` which may correlate. NOT root-caused yet. Probably the next highest-leverage code fix.
2. **Vortex IndexOutOfRangeException** — BlueOrcWarrior (29-30 May). `System.IndexOutOfRangeException at Bannerlord.ModuleManager.ApplicationVersion.TryParse`. Cross-posted to BUTR/game-mount-and-blade2#187. Filed as Nexus Bug #1086045.
3. **Moncoyoule v0.8.1 hang** — claims new campaign hangs forever when any other mod is enabled. Most likely v0.7→v0.8 load-order reversal (BetaDeps now `DependedModule`s on the 4 deps, so consumer mods load later). May be resolved by upgrade; needs runtime.log to confirm.

**P1 single-reporter, untriaged:**

4. bomberman2077 — startup crash regression after the "MyLittleWarband should be working now" reply.
5. ICEMAN6969 — campaign-map freeze (29 May) and loading-screen hang follow-up to War Sails thread.
6. Reactive4080 — hard CTD on main screen (29 May, supporter).
7. Visorak168x — crash on "next mod" button + Weapon Overhaul 1.4.5 incompat claim (30 May, premium).
8. 69Mowgli96 — constant crashes on 1.4.5, French, no log.
9. Lord0m3g4 — BLSE launcher fails (joins AFG3140 thread, 02 Jun).
10. milenski123 — CA Eagle Rising compat error, "can't read BUTR DLLs" with BLSE.
11. Rohzdear — LauncherEx incompat on Vortex (regular BLSE works).

**Nexus Bugs tab specifically:**

- #1086570 — "Vortex launch problems with BLSE" (v0.8.1, 31 May): "I get this error when trying to launch the ex version of BLSE. I only have two mods BLSE and BetaDeps." Screenshot at gyazo.com/2a7dc34a9ff50990f8a6dd6384f9e300. **2-mod minimal repro — best diagnostic value.**
- #1086045 — BlueOrcWarrior Vortex IndexOutOfRangeException (above).
- #1085125 — "No Mod Config Menu" (v0.8.0, 30 May): "There is no mod configuration menu on my new campaign". One-line report, no log. Likely fixed by v0.8.1.

---

## 4. In-progress: "every mod on one page" rewrite

### Goal

Replace the fixed-slot pagination (`SlotCount = 20`, `Page X of Y` controls) in the Mod Configuration tab with an infinitely scrollable list. Every mod gets one page; user scrolls vertically through all its settings regardless of count.

### Slices

**4a — DONE this session.** New file `src/BetaDeps.MCM/UI/PrefabExtensions/PresentationRowVM.cs`. Single-row ViewModel that holds EITHER a `SettingsPropertyVM` (real settings row) OR a header-text string (group divider). Mirrors the entire `Slot0_*` binding surface 1:1 (same names, types, behaviors): `IsVisible`, `IsHeader`, `IsProperty`, `GroupHeader`, `DisplayName`, `HintText`, `IsBool` / `IsInteger` / `IsFloating` / `IsNumeric` / `IsText` / `IsButton` / `IsDropdown`, `MinValue` / `MaxValue`, `BoolValue` + `BoolText`, `ButtonText`, `IntValue` / `FloatValue` / `ValueText` / `EditableValueText` (with parse + clamp setter), `TextValue`, `DropdownText`, `ExecuteToggleBool` / `ExecuteDropdownNext` / `ExecuteDropdownPrev` / `ExecuteAction`, `NotifyRefresh()` helper for post-preset-Apply re-paint. Uses `Bannerlord.UIExtenderEx.Attributes` for `[DataSourceMethod]` and `TaleWorlds.Library` for `[DataSourceProperty]`.

**4b — DONE this session.** `OptionsVMMixin.cs`. Added private `MBBindingList<PresentationRowVM> _rowList`, exposed as `[DataSourceProperty] public MBBindingList<PresentationRowVM> RowList`. `SelectMod` clears `_rowList` and rebuilds it from `_presentation` (the existing per-mod flattened list, one row per group-header or property) in parallel with the legacy slot fan. The old `Slot0..Slot19` data-source properties + their fan are untouched and still drive the current XML — `RowList` is purely additive so far.

**Slice 1 (diagnostic, separate from 4a-d) — DONE.** `src/BetaDeps.MCM/Internal/PresentationRowSurvey.cs`. Walks `SettingsRegistry.All`, mirrors the `_presentation` build logic, counts rows per mod, writes `Modules\BetaDeps\presentation-row-survey.log`. Gated on `Modules\BetaDeps\presentation-survey.flag`. Triggered from `OptionsVMMixin.RebuildModList` (originally `OnBeforeInitialModuleScreenSetAsRoot` but moved because that hook only fires once at game-load and the user has to drop the flag before clicking PLAY). Result for steve's modlist: max 28 rows (HomesteadsReloaded), median 14, 9 mods. ROT-class mods (200+) not in current install.

**4c — BLOCKED, needs Path α work.** The plan was to rewrite the prefab XML in `OptionsPrefabExtensions.cs`: replace the 20-row slot loop with a single `<ListPanel DataSource="@RowList">` + an `ItemTemplate` whose widget variants are gated on `@IsBool` / `@IsNumeric` / `@IsText` / `@IsDropdown` / `@IsButton` / `@IsHeader`. **Blocker** documented at the top of `OptionsPrefabExtensions.cs`:

> v0.4.19: reverted to 10-slot inline pagination (v0.4.16 working pattern). NavigatableListPanel + ItemTemplate failed to iterate CurrentRows twice (v0.4.5 with MCMOptionRow prefab include, v0.4.18 with inline ItemTemplate). UIExtenderEx mixin DataSourceProperty doesn't seem to surface MBBindingList<T> in a way NavigatableListPanel recognizes. Sticking with what works.

So the simple "point the ListPanel at `@RowList`" path was attempted twice in v0.4.5 and v0.4.18 and both times the binding system did not iterate the collection. Hypothesis: our `BetaDeps.UIExtenderEx` mixin reflection layer surfaces scalar `[DataSourceProperty]` getters into the target VM correctly, but doesn't surface `MBBindingList<T>`-typed getters in a way `ListPanel.ItemTemplate` / `NavigatableListPanel` can iterate.

**4d — not started.** Once 4c lands and we've verified the new prefab works end-to-end (header dividers, all 5 widget variants render, scrolling works, mutations stick, preset apply repaints, hint hover still fires), delete the entire `Slot0..Slot19` fan: all `Slot N_X` properties (~25 props × 20 slots ≈ 500 LOC), `_slots[]` + `_slotIsHeader[]` + `_slotGroupHeaders[]` arrays, `RefreshSlots()`, `_currentPageIndex` + `_totalPages` + `_selectedPageSummary` + `PaginationVisible` + `Page0_IsCurrent..Page9_IsCurrent`, `ExecutePrevPage` / `ExecuteNextPage`, `ExecuteSlot N_DropdownNext` / `Prev` / `ToggleBool` / `Hover` / `HoverEnd`, the SlotCount=20 const, and the matching slot-row loop in `OptionsPrefabExtensions.cs`. Pure deletion, no behavior change once 4c is verified.

### Why Path α (not β or γ)

The user picked Path α (fix the mixin → MBBindingList plumbing in our own UIExtenderEx re-implementation) because we own the entire `BetaDeps.UIExtenderEx` codebase. The most likely root cause — that the mixin's runtime reflection layer doesn't expose `MBBindingList<T>`-typed getters as iterable bindings to the parent ListPanel — is fixable in our own source. Timebox: 1 hour, fall back to Path γ (just bump `SlotCount` to 32, hide pagination UI, accept truncation for ROT-class outliers) if root cause isn't found.

---

## 5. Resume from here

### The exact step the session left on

Open `src/BetaDeps.UIExtenderEx/Runtime/` and investigate how `[DataSourceProperty]` getters on a `BaseViewModelMixin<T>` subclass get surfaced into the target VM at runtime, so we can find why `MBBindingList<T>`-typed getters don't iterate as `ListPanel.ItemTemplate` data sources.

### Quick orientation commands

```powershell
# See the mixin runtime code
ls C:\dev\beta-deps\src\BetaDeps.UIExtenderEx\Runtime\

# See what specifically intercepts ViewModel.GetPropertyValue / GetPropertyType
Get-ChildItem -Recurse C:\dev\beta-deps\src\BetaDeps.UIExtenderEx\Runtime\ -Filter *.cs | Select-String 'GetPropertyType|GetPropertyValue|MBBindingList' -List

# Look at the existing ViewModelBindingPatch (it patches ViewModel.GetPropertyValue/Type)
Get-Content C:\dev\beta-deps\src\BetaDeps.UIExtenderEx\Runtime\ViewModelBindingPatch.cs | Select-Object -First 80
```

### Expected investigation steps

1. **Confirm scalar `[DataSourceProperty]` from a mixin works through the patch.** Look at how `Slot0_DisplayName` (string) on `OptionsVMMixin` becomes visible to the prefab's `@Slot0_DisplayName` binding. Trace through `ViewModelBindingPatch`'s `GetPropertyValue` postfix.
2. **Compare against `MBBindingList<T>` case.** When `ListPanel.ItemTemplate` resolves its data source, does it call `GetPropertyValue` (which our patch handles) or `GetPropertyType` (which it may also need) or something else like `GetPropertyInfo`? If our patch only forwards `GetPropertyValue` but the iterator uses `GetPropertyType`, that explains the failure.
3. **Likely fix:** add a `GetPropertyType` postfix in `ViewModelBindingPatch` (or wherever `GetPropertyValue` is patched) that returns the mixin's property's declared type for `MBBindingList<T>` getters. May also need `GetPropertyInfo`.
4. **Quick-Test sanity check the fix:** add a minimal `<ListPanel DataSource="@RowList">` somewhere in the prefab with one trivial `ItemTemplate` widget and see if it iterates. If yes, slice 4c lands; if no, we have a deeper Gauntlet-runtime problem and fall to Path γ.

### Timebox rule

If after ~1 hour of focused investigation in `ViewModelBindingPatch` and adjacent files we can't identify why `MBBindingList<T>` doesn't iterate, FALL BACK to Path γ: bump `SlotCount` to 32 (or 50 for headroom), delete `PaginationVisible` / `Page X of Y` UI, accept that ROT-class power-user mods silently truncate at the cap. Ships value in ~1 hour; the ListPanel rewrite can come back as a v0.9 item.

---

## 6. Quick-Test workflow reference

```powershell
cd C:\dev\beta-deps

# Full build + deploy to live install + launch BLSE
.\scripts\Quick-Test.ps1

# Build without deploying (sanity check)
.\scripts\Build-Phase1.ps1 -SkipDeploy

# Skip the launch step (rebuild + redeploy only)
.\scripts\Build-Phase1.ps1
```

Quick-Test sequence: builds all 6 projects → stages to `dist\Modules\` → verifies zip-trim list + Aragas/BUTR string absence → lints XMLs → packages `dist\BetaDeps-v0.8.1.zip` → deploys to `C:\Program Files (x86)\Steam\steamapps\common\Mount & Blade II Bannerlord\Modules\` → archives previous `runtime.log` → drops `betadeps-run-selftest.flag` → launches BLSE LauncherEx → waits for `runtime.log` + `selftest.log` → copies both to `C:\dev\bannerlord\` for inspection.

Diagnostic flag files (drop in `Modules\BetaDeps\` to toggle):

- `patchshield-disabled.flag` — PatchShield doesn't install
- `saveshield-swallow-disabled.flag` — SaveShield doesn't swallow exceptions
- `auto-disable-enabled.flag` — opt-in auto-disable on crash
- `betadeps-run-selftest.flag` — auto-runs Self-Test at main menu
- `presentation-survey.flag` — runs the slice-1 presentation-row survey when Mod Configuration is opened

---

## 7. Ship sequence reference

When v0.8.2 (or next v0.8.1 retag) is ready:

```powershell
cd C:\dev\beta-deps
.\scripts\Build-Phase1.ps1 -SkipDeploy   # confirm dist\BetaDeps-v0.8.1.zip builds clean

# If reusing the v0.8.1 tag:
git tag -d v0.8.1
git push origin :refs/tags/v0.8.1
gh release delete v0.8.1 --yes

# Otherwise bump version constants in:
#   scripts/Build-Phase1.ps1 ($Version param)
#   src/BetaDeps.Foundation/BetaDeps.Foundation.csproj
#   src/BetaDeps.Harmony/BetaDeps.Harmony.csproj
#   src/BetaDeps.HarmonyHost/BetaDeps.HarmonyHost.csproj
#   src/BetaDeps.UIExtenderEx/BetaDeps.UIExtenderEx.csproj   (only the InformationalVersion)
#   src/BetaDeps.ButterLib/BetaDeps.ButterLib.csproj         (only the InformationalVersion)
#   src/BetaDeps.MCM/BetaDeps.MCM.csproj                     (only the InformationalVersion)
# UIExtenderEx / ButterLib / MCM keep their impersonation AssemblyVersion (2.13.2.0 / 2.10.4.0 / 5.11.99.0)

git add -A
git commit -m "v0.8.X: <short description>"
git tag v0.8.X
git push origin main --tags
gh release create v0.8.X dist/BetaDeps-v0.8.X.zip --title "BetaDeps v0.8.X" --notes-file ../bannerlord/nexus-changelog-v0.8.X.md
```

Then upload zip to Nexus, paste artifacts from `C:\dev\bannerlord\` into the right boxes (description, sticky, file description, changelog), and post Patreon community update.

---

## 8. Codebase orientation

### Repo layout

- `C:\dev\beta-deps\` — main repo (Trashpanda62/Betadeps on GitHub)
- `C:\dev\bannerlord\` — sibling working folder for Nexus / Patreon artifacts, inbox handoff, design docs (not in repo)
- `C:\dev\bannerwar\` — standalone Bannerwar mod (depends on BetaDeps)

### BetaDeps project structure

- `BetaDeps.Foundation` — shared infrastructure (`DiagLog`, `PatchShield`, `SaveShield`, `IncompatibleModDetector`, `SubModuleConstructionGuard`, `AssemblyVersionShim`, `MO2Bootstrap`, `BetterExceptionWindow.BEWPatch` shim, `CollectAssemblyTypesShim`)
- `BetaDeps.Harmony` — `BetaDepsHarmonySubModule` (loads Harmony binary fan + writes CREST_SHOW_STUBS + materializes alias folders)
- `BetaDeps.HarmonyHost` — produces `Bannerlord.Harmony.dll` for the real-module path (v0.8 step 1 host)
- `BetaDeps.UIExtenderEx` — clean-room re-implementation of UIExtenderEx (mixin runtime + prefab patch system + widget factory + brush manager)
- `BetaDeps.ButterLib` — clean-room re-implementation of ButterLib (DI container, HotKey manager, BEW patch host, fluent settings, ExceptionHandler)
- `BetaDeps.MCM` — clean-room re-implementation of MCMv5 + MCM.UI.Adapter + MCM Mod Configuration screen mixin
- `BetaDeps.Module` — SubModule.xml + alias SubModule.xml files
- `BetaDeps.TacticsEditor` — held back, ships in v1.0 with Bannerwar v1.0

### Key files for resumption

- `src/BetaDeps.UIExtenderEx/Runtime/ViewModelBindingPatch.cs` — installs `GetPropertyValue` / `SetPropertyValue` / `ExecuteCommand` patches on `TaleWorlds.Library.ViewModel`. **This is where Path α work begins.**
- `src/BetaDeps.UIExtenderEx/Runtime/ViewModelMixinHook.cs` (and adjacent) — `ViewModel..ctor()` postfix that attaches mixins to target VMs.
- `src/BetaDeps.UIExtenderEx/Runtime/Registration.cs` — mixin registration lookup.
- `src/BetaDeps.MCM/UI/PrefabExtensions/OptionsVMMixin.cs` — the host mixin where `RowList` is exposed.
- `src/BetaDeps.MCM/UI/PrefabExtensions/PresentationRowVM.cs` — the row VM (slice 4a output).
- `src/BetaDeps.MCM/UI/PrefabExtensions/OptionsPrefabExtensions.cs` — prefab XML, where slice 4c will land.

---

## 9. Known issues / tech debt this session noted but didn't address

- **MCM dropdown bug** (4 reporters, P0). Not yet root-caused. Next priority after the slice 4 series lands.
- **Self-Test Cancel semantics fail (0/8)** on every tested mod. Pre-existing, may correlate with the dropdown bug.
- **Quick-Test log-copy timing race** — it sometimes copies `runtime.log` before the game finishes loading (we saw this once this session). Pure Quick-Test bug, not BetaDeps.
- **Bannerwar v0.2** — `BannerwarHUDMissionLogic`, `BannerwarHUDVM`, `BannerwarHUD.xml` were created earlier this session (formation HUD overlay piece of the v0.2 plan); `FreeCameraMissionLogic` PhotoMode rewrite was already in place. Status not Quick-Tested in-mission; pending verification + commit.

---

## 10. Files modified or created in this session

### `C:\dev\beta-deps\src\` (BetaDeps repo, uncommitted)

```
src/BetaDeps.Foundation/MO2Bootstrap.cs                            (new, slice 4 of v0.8.1 retag)
src/BetaDeps.Harmony/BetaDepsHarmonySubModule.cs                   (modified — CREST_SHOW_STUBS write + MO2 bootstrap call)
src/BetaDeps.MCM/Settings/SettingsStorage.cs                       (modified — preset file-system layer)
src/BetaDeps.MCM/Settings/BetaDepsSettings.cs                      (DELETED)
src/BetaDeps.MCM/Internal/PresentationRowSurvey.cs                 (new, slice 1 diagnostic)
src/BetaDeps.MCM/UI/PrefabExtensions/PresentationRowVM.cs          (new, slice 4a)
src/BetaDeps.MCM/UI/PrefabExtensions/OptionsVMMixin.cs             (modified — preset cycle + RowList + survey trigger)
src/BetaDeps.MCM/UI/PrefabExtensions/OptionsPrefabExtensions.cs    (modified — preset cycle XML row at top of panel)
src/BetaDeps.MCM/BetaDepsMCMSubModule.cs                           (modified — removed obsolete BetaDepsSettings sync call)
scripts/Build-Phase1.ps1                                            (modified — Linux-compatible zip via ZipArchive.CreateEntry)
```

### `C:\dev\bannerlord\` (artifacts, this session)

```
nexus-description.md                                 (updated — Three ways to install section)
nexus-changelog-v0.8.1.md                            (updated — folds in BLSE/MO2/Linux/presets/raw-stack note)
nexus-file-description-v0.8.1.md                     (updated — 138 chars)
nexus-short-description-v0.8.1.md                    (new)
nexus-sticky-post-v0.8.1.md                          (new)
patreon-v0.8.1-post.md                               (new)
HANDOFF-2026-06-04.md                                (this file)
```

### `C:\dev\bannerwar\src\Bannerwar\` (Bannerwar mod, earlier in session)

```
BannerwarSubModule.cs                                 (modified — added HUD MissionLogic registration)
HUD/BannerwarHUDVM.cs                                 (new)
HUD/BannerwarHUDMissionLogic.cs                       (new)
GUI/Prefabs/BannerwarHUD.xml                          (new)
```

---

## End

Resume by opening `src/BetaDeps.UIExtenderEx/Runtime/ViewModelBindingPatch.cs` and tracing how scalar mixin-exposed `[DataSourceProperty]` getters surface to the binding layer. The exact next step is in section 5.
